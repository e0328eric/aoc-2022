# aoc-2022

Advent of Code 2022 Problem Solving using MacOS Assembly and Rust

# How to Compile and Run the Solution

It uses cargo's build script to compile both assembly and rust and linking it.
The output binary has a name `solution`.

Below command makes the solution run.

```console
cargo b && ./solution
```
